# v2 規格 H — 三鑰逃生(闖關化定案,取代事故報告框架)

> 【狀態】**設計中,待動工**(2026-08-04 使用者拍板方向,指定「先寫 spec 再動手」)。
> 【來源】玩家試玩回饋:**「雖然很多提示,但感覺很亂」**;使用者定調:
> 「這是闖關遊戲,縮減 3 個關。玩家是打工仔已經超時加班,想下班 → 老油條員工來阻止 → 打贏回收 →
> 掉落魔法鑰匙(1/3) → 領班出來奪回魔法鑰匙 → 打贏回收 → (2/3) → 老闆出來阻止 → 打贏回收 → (3/3) →
> 湊齊後順利逃生下班。**再也沒有魔法事故報告了,那是舊的想法。**」
> 【關係】**取代規格 E 的北極星**(每局產一份好笑報告)與 `docs/campaign.md`(2026-07-16 闖關決策,未動工——
> 本規格是它的收斂版,差異見 §11)。**規格 G(記錄 3 筆 → 收容終演)原封保留**,降級為「單關勝利條件」。
> 戰鬥系統(brawl-1~3.1 / 道具 / 地板化學 / 疲態 / 終演)**一行不動**——闖關是包在外面的殼。

---

## 0. 一句話

> **你是超時加班想下班的打工仔。三個同事依序擋在你和大門之間;把他打到失能三次、扔進回收艙,
> 他就掉一把魔法鑰匙。湊齊 3 把,開門下班。**

動詞完全沒變,變的是**你為什麼要做這件事**。

## 1. 為什麼換框架(病因診斷)

玩家說「提示很多但很亂」。**病根不是提示的數量,是目標本身需要被解釋。** 舊框架要玩家吞三層抽象:

| 舊框架的一層 | 玩家要自問 |
|---|---|
| 事故記錄 | 我打人為什麼叫「記錄事故」? |
| 收容封存 | 我為什麼要把同事塞進回收艙? |
| 事故報告 + S~E 等級 | 這個分數在評我什麼?我要刷高它嗎? |

新框架只有一層:**打贏擋路的人 → 拿鑰匙 → 開門下班**。三個名詞全是日常語意,零解釋成本。
而且**「把擋路的同事丟進回收艙」在打工仔框架下天然成立**——比「收容測試」更好笑,也更好懂。

> ⚠ **換框架不會自動治好「很亂」**。文案換皮只解決一半;另一半是 HUD 同時常駐太多層資訊——
> 光教練行就有 **13 條互斥分支**(`v2-hud.js:241-253`),加上標題列、AI 狀態列、橫幅、艙口 LED、
> 滿場浮字。§7 是這個規格真正的另一半藥,**不能只做換皮就收工**。

## 2. 世界留著,任務換掉(這是最省的一刀)

掃描全部文案後的關鍵發現:**場景本身完全不用改**。工業回收中心、回收艙、艙口 LED
(「回收目標已捕捉」/「分類完成:易燃魔法廢棄物」)、地板標語(`WIZARD INTAKE` / `NO MANUAL SORTING`)、
廢料名稱、道具名稱——**這些在「打工仔的工廠」框架下全部成立,一個字都不用動**。

要換的只有**「為什麼打架」那一層**:標題列、教練行、開場、橫幅、終演提示、結算畫面。

| 系統 | 現況 | 新框架 |
|---|---|---|
| 記錄 3 筆 → 收容指令 → 終演封存(規格 G) | 一局的完整循環 | **原封保留**=**單關**勝利條件 |
| 終演演出(自動抓搬拋 + 特寫 + letterbox) | 一局一次 | 保留,**尾巴接鑰匙掉落**(§5) |
| 疲態 / 立案 beat / 瀕界心跳(flow-2) | — | 保留(`f.fatigue` 由 `roundWins[1-pid]` 導出,語意天然對應「你快撐不住了」) |
| 道具 / 爆桶 / 元素站 / 拉桿 / 地板化學 / lab 場景 / 艙口 LED | — | **全部不動** |
| `AI_PROFILE` 階級 + `updateAiCall` 進場排程(tier-1) | 一場內換一次對手 | **改造成三關 boss 進場管線**(§4) |
| `applyStage`(危險物升級) | **場內即時**:比分領先就升級 | **改綁關卡**:關 1=普通、關 2=黃色警戒、關 3=全面失控 |
| **事故報告 + S~E 等級(`js/v2-report.js`)** | 每局結算 | **整組退役**(§9) |
| 「魔法事故報告 · 收容測試」框架文案 | — | **重寫**(§8) |

**戰鬥模組(v2-combat / v2-items / v2-floor / v2-terrain / render 全家)不動。**
動的是 `v2.js` 的對局編排層 + `v2-hud.js` 的呈現層 + `v2-state.js` 的幾張表。

## 3. 關卡狀態機

新增 `v2s.camp = { level: 1, keys: 0, phase: 'fight', deaths: 0 }`,取代「打完一局就 matchOver」的單局模型。

```
boot → intro ─→ fight(n) ──記滿3筆──→ order(n) ──打暈+X──→ perform(n) ──→ keydrop(n)
                   ↑                                                          │
                   │                                              ┌───────────┴──────────┐
        (敗北:自己被記滿3筆)                                   keys<3                  keys=3
                   │                                              ↓                       ↓
                retry(n) ←──────────────────────────────  handoff(n+1) → fight(n+1)    escape → clockout
```

| 狀態 | 內容 | 復用什麼 |
|---|---|---|
| `intro` | 打卡機顯示超時、「我要下班」;只有第 1 關播完整版 | `INTRO_T`/`INTRO_GO` + `CAM_INTRO→CAM_FIGHT` 混合(`v2.js:79-95`) |
| `fight(n)` | 正常戰鬥,目標=記滿 3 筆 | 現況原封(`recordIncident`,`v2-combat.js:236`) |
| `order(n)` | 收容指令(賽末點) | 規格 G 原封(`roundWins[w] >= RECORD_TARGET`) |
| `perform(n)` | 終演:自動抓搬拋 → 封存 | 規格 G 原封(`startPerform`/`finishPerform`) |
| `keydrop(n)` | **鑰匙從回收艙彈出 → 入袋**(§5) | flow-2b 印章卡飛行 |
| `handoff(n+1)` | 下一個 boss 走進場 + 一句台詞 | **`updateAiCall`(`v2-combat.js:862`)整套**——它本來就是「CALL_T 秒後在指定座標放一個新對手進來、比分保留」 |
| `retry(n)` | 「今天不用下班了」→ 重打本關 | `restartMatch` 拆一半(清 `roundWins`,**不清 `camp.keys`**) |
| `escape` | 大門/打卡機亮起 + 指引 | 新(小) |
| `clockout` | 下班結局 | 取代 `drawReport` 那張卡 |

### 三個必須寫進程式碼的分界

1. **`finishPerform` 目前是二分岔**(`v2-combat.js:830-836`):`final` → `finalSeal`→`endMatch`,否則
   → `softReintegrate`(對手復活繼續打)。新流程要改成 **`final` → `keydrop`**,而 `softReintegrate`
   那條路在闖關裡**用不到了**(單關只封存一次)——但別急著刪,`?tune`/加班模式還會用。
2. **`endMatch(pid)` 保留但脫鉤報告**:`v2s.matchOver` 不只給報告用,它還凍結 sim、擋計分、
   擋 pod pulse(`v2.js:198-201`、`v2-combat.js:237,688,751`)。**留著 `matchOver`,只拿掉 `report`**。
3. **敗北定義**:玩家自己被記滿 3 筆 → 對手對你走完終演 → `retry(n)`。
   **建議定案:重打本關,鑰匙保留,不做命數/續關幣。** 單關 1~2 分鐘,全部重來會把
   「快下班了」的張力變成挫折;portal 慣例也是死亡零懲罰。`camp.deaths` 累計當結算笑點。

### ⚠ 命名地雷:不要重用 `v2s.stage`

`v2s.stage` 現在的語意是**「由比分導出的危險等級」**(`applyStage` 被 `recordIncident` 呼叫)。
新設計想要的是**「第幾關」**——因果方向剛好相反。若直接沿用同一個欄位名,
`ring.mjs:30,48` / `finisher.mjs:31,51,53` 這幾條 `stage === 1|3` 的斷言會**用錯誤的理由通過**。

**定案:關卡索引叫 `v2s.camp.level`;`v2s.stage` 維持「危險等級」語意,但改成由 `camp.level` 直接設定
(關 n → stage n),不再由比分推。** 上述斷言一律重寫,不靠它們自己碰巧綠。

## 4. 三個 boss 檔案

`AI_PROFILE`(`v2-state.js:171`)現有旋鈕:`name / punchChance / hesitate / backoffT / grabDelay /
jumpChance / guard / comboDrop`。註解本來就寫著「之後領班/廠長=加列(campaign)」——這條路當初就鋪好了。

| 關 | 對手 | 人設 | 手感目標 | 旋鈕方向 | 場地 |
|---|---|---|---|---|---|
| 1 | **老油條員工** | 混水摸魚、賴著擋路 | 慢半拍;新手能贏 | 貼近現有 `intern`,但 `guard: true`(賴防=人設) | stage 1 普通 |
| 2 | **領班** | 管你、催你回去工作 | 壓迫感來自**場地**不是他本人 | 貼近現有 `senior` | stage 2 黃色警戒 |
| 3 | **老闆** | 最後一道關卡 | 全開,逼你用完前兩關學的東西 | `punchChance`/`jumpChance` 最高、`guard: true`、`comboDrop: 0`、`grabDelay` 最短 | stage 3 全面失控 |

**設計鐵則:三關的勝利條件完全一致**(記滿 3 筆 → 終演封存)。**不給老闆特殊規則**
(不可抓 / 鉤拳免疫 / 三段擊倒)——那是 `campaign.md` 舊版設計,會讓最後一關**要重新教一次玩法**,
正好違背「不要亂」這個目的。老闆的難度來自**旋鈕 + 場地**。

### 實作要點(掃描確認過的)

- `AI_PROFILE` 只有 `name` + 7 個行為旋鈕,**沒有顏色 / 體型 / 血量 / 立繪欄位**;
  顏色是全域 `COLORS[pid]`,名字是**可變陣列** `NAMES[1]`(`applyAiTier` 直接寫)。
  三個 boss=**加三列 + 每列多一個 `nextTier`**(現在 `updateAiCall` 把 `'senior'` 寫死在 `v2-combat.js:866`)。
- **AI 現在完全不碰道具**:`aiMove` 裡沒有任何 `pickupItem`/`useItem`/桶瓶分支;
  也**永遠不會衝刺拳**(`v2.js:301` 只有 `_fleeing` 時才給 AI `running`)。
  → **「領班愛用道具」是新行為,不是加一列旋鈕。建議先不做**(§12 加分項)。
- `v2-combat.js:935` 已有註解「counter 留給領班/廠長檔」——反擊拳目前 player-only,是老闆的天然升級點。
- **實習生逃跑(`_fleeing`)在闖關裡退役**:boss 要被你收容才會掉鑰匙,不能讓他跑掉。
  但 `updateAiCall` 這個「N 秒後在座標 P 放一個新對手進來」的排程器**整套留用**當進場管線。
  (逃跑演出本身很有戲——「老油條看你要贏了就開溜」列 §12 加分項。)

## 5. 魔法鑰匙(唯一的新玩法物件)

**掉落**:終演封存演出結束那一刻,回收艙吐出一把鑰匙(彈起 → 落在艙邊)。

**入袋——建議定案:自動飛入,不要手動撿。**
落地停留 ~0.8s 讓玩家看清楚,然後**自動飛向 HUD 的鑰匙計數器**。

- 理由:玩家的抱怨就是「東西太多、很亂」,再加一個「你必須走過去按 X」的必做步驟是逆向操作;
  漏撿還會生出一個新的卡關狀態。
- **飛行動畫是現成的**:flow-2b 的印章卡就是「生成 → 飛向計分格」(`v2s.recordCard`,`v2-hud.js:594`),
  換素材即可。
- 「掉落」的畫面感保留了(它真的掉出來、彈一下),只是不需要教一個新動作。

**顯示**:HUD 常駐 `🔑 1/3`。第 3 把入袋時整排鑰匙閃一次 + 大門亮起。
**資料**:`v2s.camp.keys`(0~3);`retry` 不清,`restartMatch`(整輪重來)才清。

## 6. 結局

3/3 → 場邊大門(或打卡機)亮起 + 一條指引 → 走過去 → **打卡下班演出**:
打卡機「嗶」→ 下班橫幅 → 走出大門。**不做總評報告、不做等級。**
結算畫面只留三件事:**通關時間**、**被回收次數**(`camp.deaths`,當笑點)、**重玩 / 回主選單**。

> ⚠ `inc.matchT`(`v2.js:215`)在報告退役後本來會變成死欄位——**留著當通關計時**。

## 7. HUD 資訊層級重整(治「很亂」的另一半藥)

原則:**任何時刻只有一個「常駐目標」+ 一個「當前障礙」**,其餘全部事件式(出現→消失)。

| 區域 | 現況 | 新 |
|---|---|---|
| 頂部標題列 `v2-hud.js:491` | 「魔法事故報告 · 收容測試　階段 1:普通　記錄 3 筆 → 收容封存」 | **`🔑 1/3　　關 2:領班擋住去路`** |
| 第二行 `v2-hud.js:496` | 「紅方:AI 同事(按 B 關掉,練手感)」 | 只在 `?tune`/練習模式顯示(正式流程它是雜訊) |
| 教練行 `v2-hud.js:241-253` | **13 條分支常駐輪播** | **分兩級**:①**動作提示**(掙脫/推開/可回收)保留=它們有時效;②**目標提示**(預設那條「C 三連擊 → 記錄 3 次事故」)**只在第 1 關**出現,之後只在**卡關**(30s 無進度)浮一次 |
| 記錄格 ×3 `drawPips` | 常駐 | 保留(它就是本關進度),語意改成「他還能撐幾次」 |
| 終演提示 `v2-hud.js:538` | 「按 X 收容!」 | 「**按 X 丟進回收艙**」(動作說完就好,不用解釋制度) |
| 結算 `drawReport` | 事故報告卡 | **退役** → 下班結算 |
| 頭頂倒三角(ui-3)、下方狀態卡(hud-1) | — | **不動** |

**新增一條規則**:教練行同一則訊息在 N 秒內不重播(現在是每幀重算最高優先那條,會一直閃)。

## 8. 文案清單

### 8.1 必須換(框架文案)

| file:line | 現況 | 換成 |
|---|---|---|
| `v2-hud.js:491` | 標題列 | `🔑 n/3　關 n:<boss>擋住去路` |
| `v2-state.js:316` | `STAGE_NAME = ['普通','黃色警戒','全面失控']` | 關卡名(草案:`加班時間`/`交接班`/`頂樓辦公室`) |
| `v2-hud.js:253` | 「C 三連擊 → 記錄對手 3 次事故」 | 「C 三連擊 → 打倒他,搶回鑰匙」 |
| `v2-hud.js:252` | 「⚠ 收容指令!打暈他 → 按 X 收容」 | 「⚠ 他撐不住了!打暈 → 按 X 丟進回收艙」 |
| `v2-hud.js:438` | 「🧑‍💼 主管:都給我好好工作!」 | 開場改「18:47。今天第 11 個小時。」 |
| `v2-hud.js:440-443` | 「打暈.打下場=記錄事故 ×3　然後收容封存他」 | 「打倒擋路的人 → 丟進回收艙 → 搶回鑰匙 ×3 → 下班」 |
| `v2.js:590` | 「⚠ 再被記一筆,對方就能對你執行收容封存」 | 「⚠ 再挨一次,今天就別想下班了」 |
| `v2-hud.js:594` | 印章卡「事故記錄 #N」 | 「還差 N 次」/ 或改鑰匙碎片視覺 |
| `v2-combat.js:250` | 「⚠ 收容指令:對 X 執行收容封存」 | 「X 撐不住了!」 |
| `v2-combat.js:701/706` | 「記錄一筆!對手墜入廢料井」/「獲勝!對手已由下層回收線清運」 | 換打工仔口吻 |
| `v2-combat.js:838` | 「最終封存完成!」 | 「🔑 鑰匙 n/3 到手」 |
| `v2-state.js:317` | `STAGE_BANNER`(臨時收容/基地警戒升級) | 關卡轉場橫幅 |
| `v2-combat.js:869` | 「資深同事到場!」 | 「領班到場!」/「老闆親自出面。」 |
| `v2-state.js:172-173` | `AI_PROFILE` name:實習生/資深同事 | 老油條員工 / 領班 / 老闆 |
| `v2.html:10` | 頁標題「環境處決熱座原型」 | 新標題 |
| `v2-report.js` 全部 | 報告文案 | **整檔刪**(§9) |

### 8.2 不用動(場景/戰鬥文案)

艙口 LED 全套(`v2-combat.js:730,801-813`)、`WASTE_CLASS`、地板標語(`render-lab.js:1367-1372`)、
`GARBAGE_NAME`、`ITEM_INFO`、以及所有戰鬥浮字(`暈!`/`擋下!`/`反擊!`/`挑飛!`/`破防!`…)。
**理由見 §2:工廠還是那個工廠,只有「你為什麼在這裡打架」變了。**

### 8.3 台詞草稿

- 老油條進場:「欸欸欸,還沒到六點吧?再幫我看一下這個。」/ 被收容:「我……我只是想找人聊天啊——」
- 領班進場:「鑰匙是公司財產。放下。」/ 被收容:「這個月的考績——」
- 老闆進場:「加班是福報。你不懂。」/ 被收容:「我不是垃圾——我是可回收物啊啊啊——」(沿用 `campaign.md`)
- 湊齊:「🔑 3/3　大門解鎖」/ 結局:「下班打卡成功。」

## 9. 事故報告退役(精確切法)

掃描已定位出**最小切除清單**:

```
刪 js/v2-report.js 整檔
刪 import: v2-combat.js:27, v2.js:27
改 v2-combat.js:851  endMatch() 拿掉 v2s.report = generateReport(pid)
刪 v2.js:466         __v2 上的 generateReport
刪 v2-hud.js:381-415 LEVEL_COL + drawReport,以及 :514 的呼叫
刪 v2-hud.js:12      import 的 inc / METHOD_ZH(兩者只剩 drawReport 在用;METHOD_COL 要留給 pips)
刪 v2.js:179 copyShare / v2.js:529 C 鍵複製 / v2-touch.js:134「複製」鈕   ← 「再戰」鈕留著
刪 v2s.report        (v2-state.js:372, v2.js:57, v2.js:470)
留 inc.matchT        (§6 當通關計時);inc.types 本來就沒人讀,可順手刪
留 containLog        (鑰匙進度 + 「被回收次數」笑點 + drawPips 在用)
留 v2s.matchOver     (它還在凍結 sim / 擋計分,不是報告專用)
```

> ⚠ 這是**規格 E 北極星的正式終結**(「每局產一份好笑到想截圖的報告」)。
> 換來「10~15 分鐘能通關、講得清楚」的闖關體驗。代價要認:**失去一個重玩動機**(刷 S 級)。
> 替代鉤子=**通關時間**(速通)+ 通關後解鎖的**加班模式**(現有無限對戰,零成本)。

## 10. 落地切法(每步照慣例:tests + 文件 + build tag + 推兩分支)

| 步 | 內容 | 主要檔案 |
|---|---|---|
| **camp-1** | 闖關殼:`v2s.camp` 狀態機 / `finishPerform` 改接 keydrop / `retry` vs `restartMatch` / localStorage 進度 | `v2.js`, `v2-state.js`, `v2-combat.js` |
| **camp-2** | 報告退役(§9 清單一次切乾淨)+ 受影響測試同步改 | `v2-report.js`(刪), `v2-hud.js`, `v2-touch.js` |
| **camp-3** | 鑰匙:掉落 → 飛入 → `🔑 n/3` + 3/3 大門解鎖 | 新 `js/v2-key.js` 或併入 `v2-items.js`, `v2-hud.js` |
| **camp-4** | 三 boss:`AI_PROFILE` 加列 + `nextTier` 鏈 + `applyStage` 綁關卡 + 進場台詞 | `v2-state.js`, `v2-combat.js` |
| **camp-5** | 文案全換(§8.1)+ HUD 層級重整(§7) | `v2-hud.js`, `v2.html` |
| **camp-6** | 結局:打卡下班演出 + 通關結算 + 加班模式解鎖 | `v2.js`, `v2-hud.js` |

## 11. 測試遷移(掃描結果:40 支裡 10 支受影響)

**好消息**:全套只有 **4 個中文字串硬釘**,而且沒有任何測試斷言標題列/教練行/`STAGE_NAME`。

| 硬釘 | 位置 |
|---|---|
| `boot.name === '實習生'` | `intern.mjs:18` |
| `senior.name === '資深同事'` | `intern.mjs:55` |
| `beat.card.phrase === '擊暈'` | `fatigue.mjs:67` |
| `/收容封存/.test(warn.banner)` | `fatigue.mjs:102` |

| 套件 | 處置 |
|---|---|
| `brawl.mjs:90-92` | **唯一因報告而整條刪的斷言**(`report.level`/`report.name`) |
| `perform.mjs:57`, `ring.mjs:54`, `finisher.mjs:81` | 各刪 `!!state().report` 那一句,其餘保留 |
| `finisher.mjs` | 終演機制(`:69,:72,:73`)**原封存活**=闖關的招牌動作;`roundWins===3`/`stage===3` 類斷言改寫成關卡語意 |
| `perform.mjs` | 拒收/罩/壓縮/雙重收容防護**全部存活**,只有 `wins[0]===4` 這種以記錄數表達的寫法要重寫 |
| `intern.mjs` | **最有價值的一支**:`:38`(逃跑中仍可被抓=不是過場)、`:47`(到出口→排程)、`:50-55`(同點進場+存活)**直接變成 boss 交接的驗收**;flee 觸發條件與名字硬釘要改 |
| `fatigue.mjs` | 疲態/冒汗/心跳全存活(`f.fatigue` 語意天然對應);`setRecords` fixture、印章卡 phrase、瀕界橫幅 regex 要改 |
| `ring.mjs` / `combo.mjs` / `ice_slide.mjs` / `jump.mjs` | 行為斷言全活,只是**觀測值**(`containLog` 的 method 標記、`roundWins` 增量)要換成新的可觀測量 |
| `onboard.mjs:32` | 依賴 `__v2.endMatch(0)` 存在;`endMatch` 保留即可,語意改「本輪結束」 |
| `hudcards.mjs` | **零遷移**(沒斷言 pips 內容) |
| 其餘 30 支 | **完全不受影響** |

> ⚠ **`brawl.mjs:28` 是地雷**:它斷言 `v2s.clockT === undefined`(B 款憲章殘留清除)。
> 「打卡下班」很可能重新引入 `clockT` 這個名字 → 會炸。動 camp-6 時記得同步改這條守衛清單。

## 12. 不做清單(範圍控制)

命數 / 續關幣、多場景美術(同場地換燈光+危險物密度)、劇情過場動畫(台詞走現有對話框/LED)、
新戰鬥動詞、雲端存檔、boss 專屬招式與特殊規則(§4 鐵則)、關卡分支/選關。

**加分項(有餘力再做,都不擋主線)**:AI 會用道具(領班)、AI 會衝刺拳/反擊拳(老闆)、
老油條快輸時逃跑演出(現成 `_fleeing` 管線)、廠長建模換皮、加班模式難度階梯。

## 13. 待拍板(有預設值,不擋工)

| # | 問題 | 預設 |
|---|---|---|
| 1 | 敗北懲罰 | 重打本關、**鑰匙保留**、無命數(§3) |
| 2 | 鑰匙入袋 | **自動飛入**,不用手動撿(§5) |
| 3 | 三關差異來源 | **只用現有 AI 旋鈕 + 場地階段**;AI 用道具列加分項(§4) |
| 4 | 關卡名 | 草案 `加班時間` / `交接班` / `頂樓辦公室`(§8.1) |
| 5 | 事故報告 | 使用者已明確退役;`containLog`/`inc.matchT` 保留他用(§9) |
